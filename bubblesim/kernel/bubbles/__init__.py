"""The Lagrangian bubble model (shared by all fidelities).

`bubble.py` is a single bubble, `forces.py` is the detachment force balance, and
`population.py` is the `Surface` population (nucleation / growth / coalescence /
detachment / advection and the coverage & void-fraction reductions).
"""
