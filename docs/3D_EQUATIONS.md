# 3D 시뮬레이터 방정식·수치기법 정리 (`bubblesim3d`)

> 2026-07-22 감사 반영 코드 기준. 최신 3세대(3D 셀 시뮬레이터)가 **실제로 풀고 있는 식**과
> **수치 방법**을 모듈별로 정리한 것. 아키텍처 개요는 [`3D_SIMULATOR.md`](3D_SIMULATOR.md),
> 커널 물리의 배경 설명은 [`../README.md`](../README.md) 참조.

## 0. 설계 원칙 — 전기화학은 재구현하지 않는다

3D 엔진은 **전기화학을 다시 유도하지 않는다.** 검증·캘리브레이션이 끝난
`bubblesim.kernel`(순수 Python, 골든 테스트로 동결)을 그대로 import해서 쓰고,
3D에서 새로 짠 것은 주로 **공간(유동·기하·다공구조) 쪽**이며, 공통 물리의 수정은
`bubblesim.kernel`에 한 번만 반영해 모든 솔버가 같은 식을 쓰도록 한다.

| 트랙 | 스케일 | 새로 푸는 것 (3D 신규) | 커널에서 가져오는 것 |
|---|---|---|---|
| **A — 셀 (라이브)** | mm, 유로/채널 | 비압축 Navier–Stokes(MAC), Lagrangian 기포 수명주기, 면 전류 재분배, 유로 복셀화 | 2전극 Butler–Volmer 전압수지, Faraday 기체율, 이탈 힘균형, 종단속도, 물성 전부 |
| **B — 포어 (배치→재생)** | µm, 외부 표면+연결 포어 | GRF 미세구조 생성, 외부 접근면 반응 마스크, 복셀 채움 기포 성장 | Faraday 기체율, 형태학(morphology) 기하 라이브러리 |

---

## 1. Track A — 셀 스케일 (`cell3d.py` 오케스트레이션)

매 스텝의 커플링 루프:

```
버블 → θ(coverage), ε(근벽 void) ─→ [커널] 2전극 전압수지 → j (8스텝마다 갱신)
버블 → α_g, d32, gas velocity ─────→ [NS] Schiller–Naumann 상간교환 → 유동장
유동장 + j ─────────────────────→ [파슬] 핵생성·성장·합체·이탈·상승·배출
```

### 1.1 스칼라 작동점 — 2전극 Butler–Volmer 전압수지
[커널 재사용: `solvers/zerod.py` `ZeroDTwoElectrodeSolver` + `kernel/kinetics.py`]

전지 전압 수지 (CA: V 고정→j 풀이 / CP: j 고정→V 계산):

```
V_cell = E_rev(T,P) + η_act,anode(j) + η_act,cathode(j) + η_conc(j) + j·R_total
```

- **반응속도 (전극별 완전 Butler–Volmer):**
  ```
  j = j0 · [ exp(α_a·F·η/RT) − exp(−α_c·F·η/RT) ]
  ```
  η(j)는 이분법으로 역산(`_invert_bv`, [0, 3 V] 브래킷).
- **버블 커버리지 → 활성면적 차단:** 국소 전류밀도가 `j/(1−θ)`로 올라가
  과전압 페널티 `(RT/α_a F)·ln(1/(1−θ))`가 전극별로 추가됨.
  **θ는 3D 파슬의 실제 접촉 발자국에서 계산**되어 스텁(`_Stub`)으로 주입
  — 기포가 덮으면 전압↑(CP)/전류↓(CA)로 되먹임이 닫혀 있음.
- **농도 과전압 (경계층 물질전달 한계):**
  ```
  η_conc = −(RT/F)·ln(1 − j/j_lim),   j_lim = 보정 base·Sherwood·Vogt closure
  ```
  여기의 전하수는 이온 운반자(OH⁻/H⁺)의 `|z|=1`이다. HER/OER 기체 분자당 전자수
  `z=2/4`는 Faraday 기체 생성에만 사용한다. `D_carrier/delta_bl` 유도 한계는 별도 proxy로
  보고하며 보정 `j_lim`에 숨겨 더하지 않는다.
- **저항 직렬합:**
  ```
  R_total = gap/κ_eff + r_membrane_area + r_contact_area
  κ_eff = κ·(1−ε)^1.5   (Bruggeman; ε = 파슬의 근벽 void)
  ```
- **캘리브레이션 기준:** AHEAD 측정 I–V (Zhang, Sci. Adv. 2026; 30 wt% KOH, 80 °C,
  zero-gap) — 기본값 `j0_HER=130`, `j0_OER=1.3e-7`(겉보기), `r_mem=3.2e-6 Ω·m²`.
- CP는 수송/물 한계를 넘겨도 설정전류를 낮추지 않고 infeasible flag와 전압 하한을 반환한다.
  CA는 한계 아래에서 bracket한다. 전기화학은 **8 유동스텝마다** 재계산하며 coupling 주기
  수렴성 시험이 필요하다.

### 1.2 전해액 유동 — 비압축 Navier–Stokes
[3D 신규: `ns3d.py`]

```
∂u/∂t + (u·∇)u = −∇p + ν∇²u + f_상간교환      ,   ∇·u = 0
```

| 요소 | 방법/식 |
|---|---|
| 격자 | **Staggered MAC** (u,v,w는 면, p는 중심) — 발산·압력구배 연산자가 일치해 투영 후 ∇·u ≈ 기계정밀도 (collocated 격자의 checkerboard 모드 회피) |
| 시간 전진 | 힘 추가 → 명시적 점성 substep → semi-Lagrangian 이류 → **마지막 Chorin 투영**. downstream parcel이 보는 최종 장의 발산을 제거한다. |
| 압력 Poisson | `∇²p = ∇·u*·h²` 를 **red-black SOR**(ω=1.7)로, 이전 스텝 p로 **warm-start** |
| 이류 | **Semi-Lagrangian** (back-trace + trilinear 보간) — 무조건 안정 |
| 상간 운동량 교환 | parcel의 `α_g`, multiplicity 기반 계면적에서 얻은 `d32`, gas-liquid slip에 Schiller–Naumann `C_d(Re)`를 적용한다. 이 경로가 준비되면 별도 Boussinesq 기포부력을 더하지 않아 이중계산을 피한다. |
| 점성 | `ν=μ/ρ`의 명시적 Laplacian, `νΔt/h²≤1/6`이 되도록 substep. 셀 엔진의 legacy 선형 `damp`는 0이다. |
| 경계 | 전극면·측벽·리브: 법선속도 0 (장애물 마스크는 압력 스텐실에서 이웃 제거 = 내부 Neumann 벽), 하단 **인렛 포트**에 u_in, 상단 p=0 개방 outlet |

### 1.3 기포 — Lagrangian 파슬 + 대표 다중도
[3D 신규 골격 + 커널 물리: `parcels.py`]

커널 `Surface`의 수명주기(핵생성→성장→이탈→상승)를 3D로 리프트. 추적 cohort의
`mult`는 **기대 기포수의 비음수 통계 가중치**이며 cohort 분할 후 1 미만도 가능하다.
**r은 진짜 단일 버블 반경**(문헌 스케일 수십~수백 µm), 가스량은 `W = mult·V(r)`.
실제 셀의 초당 10⁵~10⁶개 이탈(1:1 추적 불가능)을 통계적으로 흡수한다.

| 단계 | 식/방법 |
|---|---|
| **핵생성** | Faraday 기체 예산으로 시드 구매: `Q̇ = η_F·(j·A)/(zF)·RT/P_gas` (HER z=2, OER z=4). 시드 기본 r은 `Params.r_nuc=5 µm`; 촉매면 연속 랜덤 위치에서 land 아래를 배제한다. `mult = site_density·(0.5+β/90)·A / 140슬롯`. 이는 3D 전용 budget closure이며 `k_nuc`, `B_nuc`, `f_to_bubble`을 사용하지 않는다. |
| **성장** | 남은 예산을 부착 버블에 **r² 가중 분배**: `dV_single = dW/mult` → `r_new = (3V/4π)^{1/3}`. 성장-이탈 주기(~1 ms)가 유동 스텝(3 ms)보다 짧으므로 **서브스텝**(주기 가스량의 25%/패스, ≤32회). r이 이탈반경·채널구속에 걸리면 잉여 가스는 `mult` 재유도(= 같은 크기의 실제 버블이 더 많아짐)로 흡수 — W 보존 |
| **합체** | 벽 부착기는 `d=2r·sinβ`의 Poisson 접촉확률 증가분으로 갱신한다. 자유 swarm은 Shima식 가중 pair와 기하 collision kernel×Prince–Blanch film drainage를 쓴다. 전해질 억제는 `η=c_crit/(c_crit+c)` closure이며 보편 상수가 아니다. |
| **이탈** | `F_b + F_d + F_DEP = F_adh`의 해. 3D 기본은 실측 무유동 이탈반경 `r_departure_ref`로 adhesion을 정하고 Fritz는 fallback이다. MHD `k_mhd·jB`, DEP gradient length, `Cd_flow`, `r_min_detach`, ±30% 분산은 보정/closure 입력이다. 채널 구속 상한은 `r≤0.45·gap`. |
| **상승** | 풀린 액체속도 + Schiller–Naumann 종단 slip + Tomiyama/Antal 벽법선 drift. 계산 궤적에는 인공 sinusoidal wobble·random crawl을 넣지 않는다. 장애물 충돌은 축별 반-cell 이하 substep이며 출구에서 vent한다. |
| **되먹임** | 기체체적·계면적·gas momentum을 유효 **fluid cell만** 대상으로 3×3×3 적층해 `α_g`, `d32`, gas velocity를 만든다. raw `α_g>1`이면 세 장을 같은 셀별 비율로 축소해 반환 `α_g`·`d32`·gas velocity의 상호 일관성을 유지하고, 제거된 Euler coupling 체적을 별도 진단한다. parcel 기체 ledger 자체는 자르지 않는다. |
| **커버리지** | Poisson 합집합: `θ = 1 − exp(−Σ mult·π(r·sinβ)²/A)` (≤0.95) → 1.1의 전압수지로 |
| **보존** | `produced(∫Q̇dt) = resident(ΣW) + pending + vented`. 자유 parcel 축약은 비음수 통계 가중치로 전극 side별 `Σmult`, `Σmult·r²`, `Σmult·r³`(기대 수·계면적·체적)를 보존하며, 불가능하면 축약을 건너뛰고 진단한다. `mult≥1` 강제는 유효 충돌을 버려 시간간격 의존성을 만들므로 사용하지 않는다. |

### 1.4 전극면 전류 재분배 — 병목맵
[3D 신규, face2d 방식: `faceredist.py`]

**촉매에 부착된** 버블들의 실제 접촉 발자국 `mult·π(r sinβ)²`을 유동격자 3×3배 해상도 빈에
**디스크로 스탬핑** → Poisson 합집합 `θ = 0.9(1−exp(−면적/빈면적))` → 3×3 블러 2회 →

```
j(y,z) = j_mean · active·(1−θ(y,z)) / mean_full(active·(1−θ))
                                                    (전체 기하 면적 적분 전하 보존)
```

리브(랜드) 셀은 j=−1로 표시(전해액 없음). 정직한 한계: 공통 과전압 가정의
전극-네트워크 축약이지, 3D 갭 퍼텐셜 풀이가 아님.

### 1.5 유로 기하 복셀화
[3D 신규: `geometry.py`, `params3d.py`]

- 디자이너(serp/par/inter, 채널폭·랜드폭·채널수·깊이) → 격자 장애물 마스크.
  서펜타인은 **균일 두께 랜드 밴드 + 끝단 교대 턴 갭** = 연결된 한 줄 뱀길;
  interdigitated는 홀짝 채널 막단(dead-end); 채널은 최소 1열 개방 보장.
- **Zero-gap MEA**: 채널 층(designer 채널 깊이 → n_lay 복셀) 사이의 코어(PTL+막)는
  전부 solid — 액체는 채널에만. 촉매면 = 코어 경계(채널의 막쪽 벽).
- **인렛 포트**: 서펜타인 첫 패스의 시작단만 급수(전체 바닥면 급수 시 턴 갭에서
  면적비 ~10×의 제트가 생겨 버블이 리브를 넘는 오류 — 수정됨).
- 라이브 뷰 = 계산 격자 그대로(과장 배율 없음), 렌더러 리브는 물리 랜드 마스크에서 생성.

### 1.6 gas/liquid 비율 진단

```
Q_gas = Q̇_HER + Q̇_OER (각각 z=2, z=4의 Faraday 식),   Q_liq = u_flow·A_channel
Q_gas/Q_liq = Faraday 기체 원천 / 해결된 복셀 입구 액체유량
```

이는 설계 screening 비율이다. 현재 3D는 기체 상연속식과 기체 발생에 따른 액체 체적 치환을
풀지 않으므로 `Q_gas/Q_liq ≳ 1`을 정량 choking 기준으로 해석하지 않는다.

---

## 2. Track B — 포어 스케일 (`pore3d.py` 오케스트레이션)

프레임마다: 외부 접근면의 열린 반응 사이트 확인 → 균일 정전류 분배 →
Faraday 가스량만큼 연결 포어를 복셀 채움 → 탈출면 도달 클러스터 배출.
전기화학 반응은 외부에서 처음 만나는 촉매 표면에만 있고, 내부 침투 반응은 풀지 않는다.

### 2.1 미세구조 생성 — leveled-wave Gaussian random field
[3D 신규: `microstructure.py` + 커널 `morphology` 라이브러리 목표치]

```
백색잡음 → FFT 저역필터 exp(−2(π·corr)²k²) → 정규화 필드 f
solid = |f| < δ,  δ = quantile(|f|, solid_frac)     ← 목표 공극률 정확히 일치
```

- 레벨셋 주변 밴드 = **이중연속**(bicontinuous) 스트럿/포어 네트워크 — 금속상(전자)과
  포어상(이온·가스)이 **둘 다 관통**해야 전류·가스 솔버가 성립. flood-fill로 검증.
- 연결성 하한 `solid_frac ≥ max(0.03, 9/n)` (64³에서 폼 95% 공극률은 스트럿이
  복셀보다 얇아짐 → 하한 적용, `eps_achieved`로 명시; 128³에서 회복).
- 상관길이: carbon_paper n/14 < ni_mesh n/10 < foam n/8 (기재별 스트럿 굵기).
- 비표면적은 solid|pore 계면 면 수로 측정해 라이브러리 `a`와 대조.

### 2.2 전류분포 — 외부 접근 표면만 반응
[3D 표면 마스크: `current3d.py`]

접근 방향(y=0)에서 각 x-z 광선을 따라 처음 만나는 `pore|solid` 계면만 반응면으로 둔다.
같은 광선 뒤쪽의 내부 계면은 전류를 받지 않는다.

```
S_react = first_interface_from_access_face(solid)
I_voxel = I_total / N_open       for voxel in S_react and not gas-blocked
I_voxel = 0                       otherwise
```

- **표면 한정**: 다공전극 침투길이, roughness-factor 이용률, screened-Poisson/BV 공간
  풀이는 사용하지 않는다.
- **전하 보존**: 열린 외부 반응 복셀에 총 부과전류를 균일 분배한다.
- **차단**: 외부 반응면이 가스로 막히면 남은 외부 사이트로만 재분배한다. 전부 막히면
  전달전류와 가스 생성은 0이 된다.
- 내부 포어로 가스가 자라는 것은 기체 공간 점유이며, 내부 전기화학 반응을 뜻하지 않는다.

### 2.3 기포 성장 — 복셀 채움 (VOF 아님)
[3D 신규: `poregrowth.py` + `gastransport3d.py`]

```
목표 가스 복셀 수 = round( (produced − vented) / V_voxel ),  produced += Q̇_Faraday·dt
```

1. **성장 우선**: 기존 가스 클러스터를 열린 포어로 6-연결 팽창(고가중 프런티어 우선)
   — 물리 그림: 버블이 포어 벽을 만나면 그 연결 채널을 따라 차오름.
2. **핵생성**: 남으면 가중치 상위 표면 복셀에 시드.
   가중치 `w ∝ max(0, I_surface_voxel) · nuc_site_mult` (전류 많은 곳 × 기재 site 배수).
3. **배출**: 탈출면(분리막쪽, 전해액이 향하는 면)에 6-연결로 닿은 가스 클러스터가
   벤트 — 폼은 `escape_factor < 1`로 일부 갇힘.
4. **보존**: `produced = resident + vented` (복셀 1개 이내 오차).

Henry 용존 포화 `c_sat = P·k_H`, 과포화 `S = c/c_sat`도 커널 transport에서 제공
(핵생성 편향의 물리 근거).

---

## 3. 커널에서 공급되는 물성·상관식 (3D가 쓰는 것)

`build_context(op, params)`가 온도·농도·전해질(KOH/H₂SO₄/PB)에 맞춰 번들:

| 항목 | 내용 |
|---|---|
| `E_rev(T,P)` | 가역전압 1.229 V 기준, dE/dT ≈ −0.9 mV/K, 압력 Nernst 항 (옵션 `high_fidelity`: 수증기압·물 활동도 보정) |
| `κ(c,T)` | 전해질 전도도 (KOH ~6 M 최대; `high_fidelity`: Gilliam 2007 상관식) |
| `σ, μ, ρ_l, Δρ` | 표면장력·점도(농축 KOH 초선형)·밀도차 → Fritz·부력·종단속도에 |
| `j0(T,c), α_a, α_c` | 전극별 교환전류밀도(Arrhenius 열활성) — AHEAD 캘리브레이션 기본값 |
| `j_lim` | 경계층 물질전달 한계: `Sh = f(Re, Sc)` → 유속 의존 + Vogt 기포 증강 |
| `r_membrane_area, r_contact_area` | 막·접촉 직렬 저항 |
| `fritz_scale, k_mhd, Cd_flow, r_min_detach` | 이탈 힘균형 계수 |
| `site_density` | 핵생성 site 밀도 (× 젖음성 인자 0.5+β/90) → `mult` 유도 |
| `morphology` | 기재×나노구조 형상과 외부 접근 표면 마스크 (Track B); 내부 반응 이용률에는 사용하지 않음 |

## 4. 수치기법 한눈 요약

| 기법 | 어디에 |
|---|---|
| Staggered MAC + Chorin projection | Track A 유동 (발산 기계정밀 제거) |
| Red-black SOR (warm-start, ω=1.7) | 압력 Poisson (Track A) |
| Semi-Lagrangian 이류 + trilinear 보간 | 유동 이류(무조건 안정), 파슬 속도 샘플 |
| 이분법 (bisection) | BV 역산 η(j), CP 전류 풀이, 이탈반경 힘균형 |
| 감쇠 고정점 반복 | Schiller–Naumann 종단속도 |
| FFT 저역필터 GRF + 분위수 레벨셋 | 미세구조 생성 (정확한 공극률) |
| 배열-시프트 flood fill (6-연결) | 관통성 검사, 가스 클러스터 배출 |
| 서브스텝핑 | 벽 성장(≤32), 파슬-복셀 충돌(≤반셀/이동) |
| Poisson 통계 폐합 | 커버리지 합집합, 합체 접촉확률(증분식, dt 무관) |
| 대표 다중도 + moment 보존 축약 | 실물 크기 버블 ↔ 실물 개수(10⁵~10⁶/s) 양립 |
| 분수 스텝 캐리 | 배속 0.02×~5× 슬로모/가속 (스텝 수 내림 오차 제거) |

## 5. 정직한 한계 (모듈 docstring에 동일 명시)

### Track A — 셀 스케일
- **유동 해상도**: 점성항은 **명시적으로 있고**(`nu = mu/rho`, 무슬립 벽) 평면 채널에서
  Poiseuille 해석해를 1.5 % 이내로 재현한다. 다만 라이브 격자(h ≈ 2 mm)는 1 mm 채널의
  운동량 경계층을 **해상하지 못한다** — 채널 하나가 셀 한 칸이라 포물선이 표현될 수 없다.
  따라서 셀 뷰는 여전히 정성 시각화이고, 압력강하·Re는 정량값이 아니다.
- **기포**: sub-grid Lagrangian 파슬 — 계면 추적(VOF) 아님.
  - **two-fluid 아님**: 액체장에는 `∇·u_l=0`을 적용하지만 `α_l=1−α_g` 상연속식과
    별도 기체 연속식이 없다. 고 void에서 액체유량·holdup·압력강하를 동시에 닫지 못한다.
  - **이탈 후 성장 없음** (자유 기포의 반경은 합체로만 변한다).
  - **난류 분산 없음**: 이 셀은 층류(Re ≈ 4×10²)라 Prince–Blanch의 난류 충돌항과
    소산율 ε_t가 적용되지 않는다.
  - **성장 부스텝 상한 32**: 씨앗→이탈 사이클(~1 ms)이 유동 스텝(3 ms)보다 짧아 벽 배열만
    부스텝하는데, 아주 얇은 채널·고전류에서는 상한에 걸려 사이클이 미해상이 된다
    (이때 부착 기포 수·피복률 θ가 0에 가깝게 나오는 것은 정보의 부재이지 아티팩트가 아니다).
  - **무리 합체의 닫힘식(closure) 1개**: 셀 평균 보이드율을 실제 근벽 시트(두께 ≈ 2 y_w)로
    농축하고 랜덤 최밀충전(ε_max = 0.62)에서 자른다. 두 경계 모두 **답을 지배하지 않는다**
    (농축계수를 1↔200으로 흔들어도 평균 기포 크기 변화 < 12 %, 10 이상은 ε_max가 포화).
- **이탈 반경 하한(`r_min_detach`)**: 1 mm 채널·펌프 1 m/s에서 힘균형이 요구하는 크기가
  커널이 모델링하는 최소값보다 작아 **바닥값(8 µm)에 닿는다**. 물리적 이탈 크기가 아니라
  **모델 바닥**이다. u ≤ 0.6 m/s에서는 바닥과 무관하게 매끄럽게 변한다.
- **국소 유동 방향은 위치의 함수**: 사행 유로 음극면(|u| > 20 mm/s인 셀 373개) 실측 —
  패스 중앙은 거의 수평(75~90°: 87%), **턴 갭은 거의 수직 상승**(0~15°: 5%, v_y ≈ +350 mm/s,
  v_z ≈ 0), 나머지 8%는 코너의 비스듬한 흐름. 패널 ①은 클릭 지점의 `vel3d`를 그대로 읽어
  부력·유동·합을 셀 좌표계 나침반에 그린다.
- **면 전류맵**: 공통 과전압 전극-네트워크 축약(부착기포 θ 기반 재분배). 전체 기하 면적의
  적분전류는 0D 평균전류와 같지만, 3D 갭 퍼텐셜 풀이가 아니고 gas source에 재입력되지 않는다.
- **포트**: 입·출구를 아래·위·왼쪽·오른쪽 **네 변** 중에서 고를 수 있다. 관통 방향(x)
  포트는 없다 — 전극면 자체이기 때문.
- **교차형(interdigitated)은 이 모델에서 성립하지 않는다**: 실제 교차형 판은 채널을 막다른
  길로 만들고 액체를 **다공 수송층을 관통**시켜 보내는데, 여기서는 코어가 고체라 입구→출구
  경로가 아예 없다. 실측: Q_in = 1.1e-5, Q_out = **0**, 질량수지 100 % 어긋남, 발산 0.30
  (허용치의 150배), SOR가 매 스텝 상한을 소진. 그래서 `flow_ok = false`로 **검출**해 UI에
  경고를 띄우고, 템플릿 갤러리에서는 제외했다.
- **격자는 유로 형식과 무관**: 두 면내 축 모두 정수 패스 주기로 스냅한다. `ff`에 따라 스냅하면
  프리셋 → 직접 그리기로 바꾸는 순간 격자가 바뀌어 그림이 리샘플되고 6 mm 리브가 8 mm로 뭉개진다.
- **직접 그린 유로**: 에디터가 **물리 격자 해상도 그대로** 그린다(한 칸 = 솔버 셀 하나).
  전극 크기·채널 깊이를 바꿔 격자가 바뀌면 그림을 **면적 가중**으로 한 번 옮긴다(반 셀보다
  얇은 리브는 그때 사라질 수 있음). 전부 리브로 칠하면 채널 한 줄을 강제로 열어 둔다.
  (과거: 고정 32×32 캔버스를 최근접 리샘플 → 32행을 25셀 격자에 내릴 때 7행을 **아예 샘플하지
  않아** 캔버스 행의 22 %에 그린 1셀 리브가 물리에 존재하지 않았다.)
- 스칼라 전기화학은 **0D** (2세대 앱의 다공 깊이/1D/채널 솔버와는 별개 축약 수준).

### Track B — 포어 스케일
- **전류**: 접근면에서 처음 만나는 외부 표면에만 균일 정전류를 분배한다.
  내부 퍼텐셜·BV·반응 침투는 계산하지 않는다.
- **기포 성장**: 복셀 채움(volume-filling) — 계면 추적 아님.
- **미세구조**: 통계적 대표 구조(목표 공극률 정확) — 특정 전극의 토모그래피 재구성 아님.
  폼은 64³ 해상도에서 연결성 유지를 위해 고체분율 하한을 적용(`eps_achieved`로 명시).

## 6. 검증 앵커 (`tests/`)

숫자는 모두 현재 코드에서 실측한 값이다.

- **골든 동결**: `bubblesim/` 무수정 → `test_characterization` rel 1e-9로 bit-identical.
- **보존 (정확한 것)**
  - 가스 폐합 `produced = resident + pending + vented` — Track A 기계정밀,
    Track B 복셀 1개 부피 이내.
  - 대표 다중도 항등식 `W = mult·V(r)`과 축약 전후 side별 수·면적·체적 moment 보존.
  - 면 전류 재분배: **전체 기하 면 평균** = `j_mean`; land는 0.
- **유동 (해석해 대조)**
  - **평면 Poiseuille**: 무슬립 채널이 `u(x) = 1.5 ū [1-((x-L/2)/(L/2))²]`를 **최대 1.5 %**
    오차로 재현(Re = 4e-3, Stokes). `nu = 0`이면 플러그 그대로 → 포물선은 **점성항**이
    만든 것이지 수치확산이 아님.
  - 압력 풀이: 차가운 닫힌 상자 RB-SOR 1000 sweep → max|div| **3.5e-14**(기계정밀).
  - **라이브 루프**(warm-start, `PROJ_TOL = 2e-3`, cap 80): 사행 유로에서는 cap이 먼저
    걸릴 수 있다. 최종 `div·h/v_max`를 `projection_relative_divergence`로 매 snapshot 보고하고,
    tolerance를 넘으면 `projection_converged=false`와 UI 경고를 낸다. 스텝 순서는
    `forces → 점성 → 이류 → 투영`; 제한된 sweep 결과를 정량 수렴으로 간주하지 않는다.
- **기포 물리 (엔진의 실제 설정에서)**
  - 이탈 반경, θ=30/60/120° · **정지 액체**: 61 / 122 / 244 µm (젖음성 레버 4×).
  - 같은 각, **펌프 0.35 m/s · 채널 깊이 1 mm**: 32 / 40 / 43 µm — 큰 기포일수록 전단층에
    더 들어가 항력을 더 받으므로 레버가 **1.3×로 자기제한**된다.
  - 유속 스윕(θ=60°): 122 → 79 → 40 → 15 µm (u = 0 / 0.2 / 0.35 / 0.6 m/s), 단조 감소.
  - **벽면-법선 힘균형**: Tomiyama 양력(Eo_d ≈ 1e-3 → C_L > 0) + Antal 벽 윤활력.
    양력이 모든 이격거리에서 이기므로 기포는 전극에 **눌려** `y_w ≈ 1.05 r`에 앉는다.
    표면이 전극 안으로 들어가는 기포는 0개. (Eo_d > 4에서는 C_L 부호가 뒤집혀 mm 기포가
    중심부로 이동 — 상관식 두 분기 모두 테스트.)
  - **합체**: 벽면(접촉확률 증분식)과 자유 무리(Prince–Blanch 충돌 커널 × 배수 효율) 둘 다.
    두 경로 모두 스텝 크기에 무관(dt 4× 세분 → 합체수 1.7배 이내).
    **출구 기포 크기**: 6 M KOH **≈ 42 µm**, 0.1 M **≈ 84 µm**(p95 210 µm) —
    salting-out 억제의 실측 가능한 결과. 합체가 활발하면 기포가 빨리 떠올라 **holdup이 감소**.
  - 벽 구속: 어떤 기포도 0.45·채널깊이를 넘지 않음.
  - 종단속도 = 커널 Schiller–Naumann과 일치(rel 1e-3), 촉매면(막쪽)에서 핵생성,
    부착 기포는 자기 반경만큼 벽에 접함, 미세구조 공극률·관통성.
- **경계조건**
  - **유동 경로 검출**: 입구 포트에서 출구 포트까지 열린 채널이 이어지지 않으면 `flow_ok=false`.
    사행·병렬은 참, 교차형은 거짓, 틈 없는 리브를 그으면 거짓 (`test_dead_plates_are_detected`).
  - 입·출구 포트는 네 변 어디에나: 지정 법선속도(입구)와 Dirichlet p=0(출구),
    나머지는 플레이트. 측면 관류에서 **질량수지 5 % 이내**, 기체도 그 포트로만 배출
    (윗면을 막으면 기포가 갇혀 옆으로 기어가 포트를 찾는다).
- **외부 앵커**: 2전극 기본값이 AHEAD 2.0 V 측정점 0.785 A/cm²를 ±12%로 확인하고
  1.6/1.8/2.0 V 단조성과 넓은 범위를 검사한다. 전 곡선 수% 검증은 아니다.
  상세는 [`../VALIDATION.md`](../VALIDATION.md), 감사 반영은
  [`PRO_AUDIT_IMPLEMENTATION.md`](PRO_AUDIT_IMPLEMENTATION.md).
